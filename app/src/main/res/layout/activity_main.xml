<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.camera.view.PreviewView
        android:id="@+id/pv_main"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        app:layout_constraintTop_toTopOf="@id/pv_main" />

    <Button
        android:id="@+id/btn_show_pic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="点击截图"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pv_main" />

    <ImageView
        android:id="@+id/img_pic"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginStart="10dp"
        app:layout_constraintStart_toEndOf="@id/btn_show_pic"
        app:layout_constraintTop_toTopOf="@id/btn_show_pic" />

    <Button
        android:id="@+id/btn_save_pic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="保存图片"
        app:layout_constraintStart_toStartOf="@id/btn_show_pic"
        app:layout_constraintTop_toBottomOf="@id/btn_show_pic" />


</androidx.constraintlayout.widget.ConstraintLayout>